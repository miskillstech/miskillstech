version: '2'
services:
  nginxdocker:
    build: ./
    volumes:
       - /ms/miskillstech.com:/ms/miskillstech.com
       - /ms/miskillstech.com/code/runenv-config/ms/sites-enabled/default:/etc/nginx/sites-enabled/default
       #- /var/run/docker.sock:/var/run/docker.sock:ro        # needed for docker events
    ports:
       - "80:80"
